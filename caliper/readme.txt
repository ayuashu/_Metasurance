1. First install NPM v10.22.1

2. Run the generate.sh to install Hyperledger Caliper CLI tool and generate the network_config.json file.

    $ bash generate.sh

3. Run the run.sh to run the benchmark.

    $ bash run.sh

4. Check report.html once the test runs successfully!