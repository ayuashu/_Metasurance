const CreateAsset = require('./createAsset');
const QueryAsset = require('./queryAsset');
const QueryAllAssets = require('./queryAllAssets');
const DeleteAsset = require('./deleteAsset');
module.exports = {
    CreateAsset,
    QueryAsset,
    QueryAllAssets,
    DeleteAsset
}