const RegisterVerifier = require('./registerVerifier');
const ReadProfile = require('./readProfile');
const Login = require('./login');

module.exports = {
    RegisterVerifier,   
    ReadProfile,
    Login
}